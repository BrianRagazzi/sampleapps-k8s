apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: survive3
  name: survive3
spec:
  replicas: 1
  selector:
    matchLabels:
      run: survive3
  strategy: {}
  template:
    metadata:
      labels:
        run: survive3
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
      volumes:
      - name: vol-nfs-survive3
        persistentVolumeClaim:
          claimName: vol-nfs-survive3-claim
      - name: vol-backup-survive3
        persistentVolumeClaim:
          claimName: vol-backup-survive3-claim
      containers:
      - image: itzg/minecraft-server
        name: survive3
        securityContext:
          runAsUser: 1000
          runAsGroup: 1000
        envFrom:
        - configMapRef:
            name: survive3-config
        resources:
          requests:
            memory: "2Gi"
        ports:
        - containerPort: 25565
          name: main
        readinessProbe:
          exec:
            command:
            - mcstatus
            - localhost
            - ping
          initialDelaySeconds: 240
          periodSeconds: 5
        livenessProbe:
          exec:
            command:
            - mcstatus
            - localhost
            - ping
          initialDelaySeconds: 240
          periodSeconds: 5
        volumeMounts:
          - mountPath: "/data"
            name: vol-nfs-survive3
      - name: cloudcmd
        image: coderaiser/cloudcmd
        volumeMounts:
          - mountPath: "/data"
            name: vol-nfs-survive3
        ports:
        - containerPort: 8000
          name: cloudcmd
        args:
          - --root
          - /data
          - --name
          - survive
          - --one-file-panel
        securityContext:
          runAsUser: 1000
      - image: harbor.pae.ragazzilab.com/library/mc-backup:1.0.3
        name: backup
        securityContext:
          runAsUser: 1000
          runAsGroup: 1000
        envFrom:
        - configMapRef:
            name: survive3-config
        volumeMounts:
          - mountPath: "/data"
            name: vol-nfs-survive3
            readOnly: true
          - mountPath: "/backups"
            name: vol-backup-survive3
