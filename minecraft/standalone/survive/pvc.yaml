apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-nfs-survive-claim
spec:
  selector:
    matchLabels:
      mcserver: survive
  accessModes:
  - ReadWriteOnce
  resources:
     requests:
       storage: 5Gi
