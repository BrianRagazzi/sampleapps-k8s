---
apiVersion: v1
data:
  EULA: "true"
  VERSION: LATEST #1.14.4 # 1.12.2
  FORGEVERSION: RECOMMENDED
  TYPE: PAPER # VANILLA, FORGE, SPIGOT, PAPER
  SERVER_NAME: "survive2"
  MEMORY: "2G"
  BUILD_FROM_SOURCE: "true" #Spigot
  MOTD: "survive2 - PaperMC on K8s"
  ONLINE_MODE: "false" # false for bungeecord
kind: ConfigMap
metadata:
  creationTimestamp: null
  name: survive2-config
---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: vol-nfs-survive2
  labels:
    type: nfs
    mcserver: survive2
spec:
  storageClassName: nfs-survive2
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: 192.168.96.20
    path: /nfs/kubevols/survive2
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vol-nfs-survive2-claim
spec:
  storageClassName: nfs-survive2
  selector:
    matchLabels:
      mcserver: survive2
  accessModes:
  - ReadWriteOnce
  resources:
     requests:
       storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    run: survive2
  name: survive2
spec:
  replicas: 1
  selector:
    matchLabels:
      run: survive2
  strategy: {}
  template:
    metadata:
      labels:
        run: survive2
    spec:
      securityContext:
        runAsUser: 1000
        runAsGroup: 1000
      volumes:
      - name: vol-nfs-survive2
        persistentVolumeClaim:
          claimName: vol-nfs-survive2-claim
      containers:
      - image: itzg/minecraft-server
        name: survive2
        envFrom:
        - configMapRef:
            name: survive2-config
        resources:
          requests:
            memory: "2Gi"
        ports:
        - containerPort: 25565
          name: main
        readinessProbe:
          exec:
            command:
            - mcstatus
            - localhost
            - ping
          initialDelaySeconds: 240
          periodSeconds: 5
        livenessProbe:
          exec:
            command:
            - mcstatus
            - localhost
            - ping
          initialDelaySeconds: 240
          periodSeconds: 5
        volumeMounts:
          - mountPath: "/data"
            name: vol-nfs-survive2
---
apiVersion: v1
kind: Service
metadata:
  name: survive2-lb
spec:
  type: LoadBalancer
  ports:
  - port: 80
    name: dynmap
    protocol: TCP
    targetPort: 8123
  - port: 25565
    name: minecraft
    protocol: TCP
    targetPort: 25565
  selector:
    run: survive2
